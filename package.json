{
  "name": "agent-juice",
  "private": true,
  "version": "0.1.6",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=4 src/*.test.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
